name: Sync to HF Space (placeholder)
on:
  workflow_dispatch:

jobs:
  sync:
    if: false
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "HF Space sync wired up in a later phase."
